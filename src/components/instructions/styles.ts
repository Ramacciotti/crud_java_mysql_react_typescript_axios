import styled from 'styled-components';

export const Content = styled.div`
  display: flex;
  width: 90%;
  flex-direction: column;
  align-items: center;

  h2 {
    color: var(--white);
  }

  div {
    display: flex;
    width: 100%;
    flex-direction: column;
  }

  p {
    color: var(--white);
  }
`;
